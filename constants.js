// const is used when value doesn't change later on the code
// example value of pi is constant and cannot be changed

let radius = 5
const pi = 3.14 //value cannot be changed later
let area

area = pi * radius * radius
console.log(area)