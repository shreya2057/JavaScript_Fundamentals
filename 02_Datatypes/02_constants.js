// Constants are the variables whose value doesn't change later on the code.

// const keyword is used define and create constant variable.

// For example value of pi is constant and cannot be changed.

let radius = 5
const pi = 3.14 //value cannot be changed later
let area

area = pi * radius * radius
console.log(area)