// Array -> Storing data in sequential order
//       -> The first index of array is 0
//       -> The data of the array can be accessed through the index of the array   

// Defining empty array
let numbers = new Array()
var characters = []

var user = ["user1", "user2", "user3"]
console.log(user)
console.log(user[0])
console.log(user[2])
